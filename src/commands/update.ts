export default async function updateCommand(_args: string[]): Promise<void> {
  console.log("`skills update` is reserved and coming soon.");
  console.log(
    "Per-skill version tracking is required before deterministic updates are implemented.",
  );
}
