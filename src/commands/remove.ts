export default async function removeCommand(_args: string[]): Promise<void> {
  console.log("`skills remove` is reserved and coming soon.");
  console.log(
    "Per-skill version metadata is required before safe removal workflows are implemented.",
  );
}
